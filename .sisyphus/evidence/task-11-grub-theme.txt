Task 11: Re-add distro-grub-themes as aspect
==================================================
Date: 2026-02-21
Status: PASS

Files created:
- modules/aspects/boot-grub-theme.nix

Files modified:
- modules/hosts/nixos-desktop.nix (moved grub theme from external import to dendritic aspect)

Changes:
1. Created boot-grub-theme.nix aspect wrapping inputs.distro-grub-themes.nixosModules.x86_64-linux.default
2. Added boot-grub-theme to self.modules.nixos aspect list in nixos-desktop host
3. Removed direct distro-grub-themes import from external modules block

Verification:
$ nix eval .#nixosConfigurations.nixos-desktop.config.boot.loader.grub.enable
true

Note: New file required `git add` before nix eval (flakes only see tracked files).
NixOS-only: aspect only defines flake.modules.nixos (no darwin equivalent).

Task: 7 - Activate pomodoro module
File modified: modules/roles/desktop.nix

Added `programs-pomodoro` to the `nixosAspects` list in `roles-desktop`.
This is the correct activation point because pomodoro is a program associated with the Waybar component, which is managed as part of the desktop role. 
By importing it here, it will be automatically included in the `nixos-desktop` host configuration, satisfying the dendritic pattern where roles compose leaf modules.
The module is not orphaned because it is now reachable via the `roles-desktop` import chain imported by the host.